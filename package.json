{
  "scripts" : {
    "validate": "npx eslint --fix *.js && npx prettier --write *.js && npx prettier --check *.js"
  },
  "dependencies": {
    "eslint-config-standard": "^17.0.0",
    "prettier": "^2.8.3"
  },
  "devDependencies": {
    "eslint": "^8.32.0"
  }
}
